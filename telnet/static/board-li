 {{ pid|string|width(4) }} {{ flag|post_mark }} {{ ' ' if manager.readmark.is_read(user.userid, manager.board.id2name(bid),pid) else 'N' }}   {{ owner|width(-15) }}{{ posttime.strftime("%b %d") }}    {{ '● ' if replyid == 0 }}{{ title }}
